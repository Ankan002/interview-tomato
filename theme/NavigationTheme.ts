import { DarkTheme } from "@react-navigation/native";

export const NavigationTheme = {
    ...DarkTheme,
    colors: {
        ...DarkTheme.colors,
        card: '#000000',
        border: '#FFC600'
    }
}